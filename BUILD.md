# iPOS Zen — دليل البناء والإنتاج

## إصلاحات الاستقرار (مايو 2025)

### 🔴 نظام الـ PWA والأوفلاين (مكتمل)
- إضافة `public/service-worker.js` اليدوي لتفعيل مستمع الـ `fetch`.
- ضبط `next.config.js` لدعم `output: export` بانسجام مع محرك الأوفلاين.
- تحصين هوك `useDebounce` لمنع تجمد الواجهة في المتصفحات.

### 🟡 نظام الطباعة السيادي (مكتمل)
- **Monochrome A4**: إزالة الألوان وتصغير الخطوط لتوفير الحبر وضمان الاحترافية.
- **Thermal 80mm**: تحسين الكثافة والمحاذاة باستخدام خطوط `Monospace`.
- **AutoPrint**: تفعيل الطباعة التلقائية الفورية بعد كل عملية بيع (قابل للضبط من الإعدادات).

### 🟢 التوافق والأداء
- الترقية لـ Next.js 15 (Turbopack) لسرعة بناء فائقة.
- إزالة كافة أنواع `any` من محرك الخدمات (Services) لضمان استقرار النوع (Type Safety).

---

## تشغيل البناء للإنتاج

### 1. بناء نسخة الويب (PWA)
```bash
npm run build        # يُنتج مجلد out/ جاهز للاستقرار على أي خادم استاتيكي
```

### 2. بناء نسخة المكتب (Windows EXE)
```bash
npm run electron:build # بناء ملف EXE احترافي للويندوز داخل مجلد dist/
```
